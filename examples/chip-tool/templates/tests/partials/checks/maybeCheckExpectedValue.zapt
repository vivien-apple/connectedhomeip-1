{{~#if hasExpectedValue}}
    {{~# if parent.isCommand~}}
        {{~>valueEquals actual=(concat "value." (asLowerCamelCase name)) label=(asLowerCamelCase name) expected=expectedValue depth=0~}}
    {{~else~}}
        {{~>valueEquals actual="value" label=(asLowerCamelCase name) expected=expectedValue depth=0~}}
    {{~/if~}}
{{~/if~}}
